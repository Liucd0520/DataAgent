
translate_prompt_template = """
# 指令
请将以下数据库字段名从中文翻译为符合数据库命名规范的英文字段名。要求如下：
使用小写字母，单词之间用下划线 _ 分隔（snake_case 风格）；
字段名应简洁、语义明确，优先使用通用技术术语或行业标准词汇；
避免使用缩写，除非是广泛认可的（如 id、url、email 等）；
保持一致性（例如，“用户”统一译为 user，而非有时用 customer）；
不要包含特殊字符或空格。
不要输出任何解释，只需要输出对应的英文字段

# 该字段为：
{field_name}

# 输出：
"""